<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>一文讲懂模拟器抓包App</title>
      <link href="/post/emulator-packet-capture/"/>
      <url>/post/emulator-packet-capture/</url>
      
        <content type="html"><![CDATA[<blockquote><p>在模拟器上抓包 App，核心思路就两件事：<strong>让模拟器的网络流量经过电脑上的抓包工具</strong>，<strong>解决 HTTPS 证书信任问题</strong>。</p></blockquote><h2 id="原理：流量为什么能”转弯”"><a href="#原理：流量为什么能”转弯”" class="headerlink" title="原理：流量为什么能”转弯”"></a>原理：流量为什么能”转弯”</h2><p>正常情况下，App 直接与服务器通信。抓包就是要在中间插一个”收费站”：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">App ──→ 抓包工具(Charles/Burp) ──→ 目标服务器</span><br><span class="line">         ↑ 你在这里看全部流量</span><br></pre></td></tr></table></figure><p>具体到 HTTPS：抓包工具会伪造一张证书（用它的根证书签发），App 如果信任了这张证书，流量就会被解密成明文供你分析。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">┌──────────┐   HTTPS(证书B)   ┌────────────┐   HTTPS(证书A)   ┌──────────┐</span><br><span class="line">│  模拟器   │ ───────────────→ │  抓包工具   │ ───────────────→ │  服务器   │</span><br><span class="line">│  信任B    │ ←─────────────── │  信任A      │ ←─────────────── │          │</span><br><span class="line">└──────────┘  明文在这里可见    └────────────┘                  └──────────┘</span><br></pre></td></tr></table></figure><p>所以整个流程 &#x3D; <strong>配置代理</strong>（流量转向）+ <strong>安装证书</strong>（建立信任）。</p><h2 id="⚙️-准备：选对工具是成功的一半"><a href="#⚙️-准备：选对工具是成功的一半" class="headerlink" title="⚙️ 准备：选对工具是成功的一半"></a>⚙️ 准备：选对工具是成功的一半</h2><h3 id="模拟器推荐"><a href="#模拟器推荐" class="headerlink" title="模拟器推荐"></a>模拟器推荐</h3><table><thead><tr><th>模拟器</th><th>特点</th><th>备注</th></tr></thead><tbody><tr><td><strong>MuMu 模拟器</strong></td><td>支持 ROOT、可写系统盘</td><td>ADB 调试端口默认 <strong>16384</strong>，官方文档详细</td></tr><tr><td><strong>夜神 Nox</strong></td><td>支持 ROOT，Android 5.1 镜像方便</td><td>老版本系统规避高版本证书限制，操作简单</td></tr><tr><td><strong>Genymotion</strong></td><td>面向开发的虚拟机</td><td>基于 VirtualBox，适合自动化</td></tr></tbody></table><blockquote><p>建议使用 <strong>NAT 网络模式</strong>，代理设置会容易很多。抓包尽量用 Android 5.1 ~ 9 的镜像，证书和 SSL Pinning 的处理会省不少事。</p></blockquote><h3 id="抓包工具选择"><a href="#抓包工具选择" class="headerlink" title="抓包工具选择"></a>抓包工具选择</h3><table><thead><tr><th>工具</th><th>定位</th><th>适用场景</th></tr></thead><tbody><tr><td><strong>Charles</strong></td><td>图形界面友好，功能强大</td><td>最主流的选择，新手友好</td></tr><tr><td><strong>Fiddler</strong></td><td>Windows 经典抓包工具</td><td>配置思路与 Charles 类似</td></tr><tr><td><strong>Burp Suite</strong></td><td>Web 安全测试的瑞士军刀</td><td>安全分析、渗透测试场景</td></tr><tr><td><strong>mitmproxy</strong></td><td>命令行工具</td><td>自动化脚本、批量处理</td></tr><tr><td><strong>HttpCanary</strong></td><td>运行在模拟器内的 App</td><td>抓取特定应用流量，免电脑</td></tr></tbody></table><h2 id="📡-第一步：配置代理，让流量”转弯”"><a href="#📡-第一步：配置代理，让流量”转弯”" class="headerlink" title="📡 第一步：配置代理，让流量”转弯”"></a>📡 第一步：配置代理，让流量”转弯”</h2><p><strong>1. 开启抓包工具的代理端口</strong></p><ul><li><strong>Charles</strong>：<code>Proxy → Proxy Settings</code>，默认端口 <strong>8888</strong>，勾选 Enable transparent HTTP proxying</li><li><strong>Fiddler</strong>：<code>Tools → Options → Connections</code>，默认端口 <strong>8888</strong></li><li><strong>Burp Suite</strong>：<code>Proxy → Proxy Listeners</code>，添加监听 <code>127.0.0.1:8080</code>，勾选 Support invisible proxying</li></ul><p><strong>2. 获取宿主机 IP</strong></p><p>在电脑命令行执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Windows</span></span><br><span class="line">ipconfig</span><br><span class="line"></span><br><span class="line"><span class="comment"># Mac / Linux</span></span><br><span class="line">ifconfig</span><br></pre></td></tr></table></figure><p>找到局域网 IP（如 <code>192.168.1.100</code>）。<strong>注意</strong>：在模拟器内部，<code>10.0.2.2</code> 固定指向宿主机（你的电脑），相当于”回环地址”。</p><p><strong>3. 设置模拟器的 Wi-Fi 代理</strong></p><p><code>设置 → 网络和互联网 → Wi-Fi</code> → 长按当前 Wi-Fi → 修改网络 → 代理设为<strong>手动</strong>：</p><ul><li>代理主机名：<code>10.0.2.2</code>（或电脑局域网 IP）</li><li>代理端口：<code>8888</code>（或你工具的端口）</li></ul><p>也可以直接用 ADB 命令行设置，脚本化更方便：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 设置代理</span></span><br><span class="line">adb shell settings put global http_proxy 10.0.2.2:8888</span><br><span class="line"></span><br><span class="line"><span class="comment"># 清除代理</span></span><br><span class="line">adb shell settings put global http_proxy :0</span><br></pre></td></tr></table></figure><h2 id="🔐-第二步：安装证书，解锁-HTTPS"><a href="#🔐-第二步：安装证书，解锁-HTTPS" class="headerlink" title="🔐 第二步：安装证书，解锁 HTTPS"></a>🔐 第二步：安装证书，解锁 HTTPS</h2><h3 id="为什么必须装”系统证书”？"><a href="#为什么必须装”系统证书”？" class="headerlink" title="为什么必须装”系统证书”？"></a>为什么必须装”系统证书”？</h3><p>Android 7.0 以上，App 默认<strong>只信任系统证书</strong>，你在手机上”安装证书”装的用户证书对多数 App 无效。所以必须把抓包工具的根证书装进系统证书目录 <code>/system/etc/security/cacerts/</code>——这需要 <strong>ROOT 权限</strong>和<strong>可写系统盘</strong>。</p><h3 id="通用操作流程"><a href="#通用操作流程" class="headerlink" title="通用操作流程"></a>通用操作流程</h3><p><strong>1. 从抓包工具导出证书</strong></p><ul><li>Charles：<code>Help → SSL Proxying → Save Charles Root Certificate</code>，保存为 <code>charles.pem</code></li><li>Fiddler：<code>Tools → Options → HTTPS</code> → 勾选 Decrypt HTTPS traffic → <code>Actions → Export Root Certificate to Desktop</code></li><li>Burp：<code>Proxy → Proxy Settings → Import/Export CA certificate</code></li></ul><p><strong>2. 转换格式并计算哈希重命名</strong></p><p>Android 系统证书的文件名是证书主题的 <strong>哈希值 + <code>.0</code></strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 计算证书哈希（以 Charles 为例）</span></span><br><span class="line">openssl x509 -inform PEM -subject_hash_old -<span class="keyword">in</span> charles.pem -noout</span><br><span class="line"><span class="comment"># 输出形如：d0c556f7</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重命名证书文件</span></span><br><span class="line"><span class="built_in">mv</span> charles.pem d0c556f7.0</span><br></pre></td></tr></table></figure><p><strong>3. 通过 ADB 推送到系统证书目录</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">adb root          <span class="comment"># 获取 root 权限（模拟器需开启 ROOT）</span></span><br><span class="line">adb remount       <span class="comment"># 使系统分区可写（或开启&quot;可写系统盘&quot;）</span></span><br><span class="line"></span><br><span class="line">adb push d0c556f7.0 /system/etc/security/cacerts/</span><br><span class="line">adb shell <span class="built_in">chmod</span> 644 /system/etc/security/cacerts/d0c556f7.0</span><br><span class="line">adb reboot        <span class="comment"># 重启模拟器让证书生效</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>夜神小技巧</strong>：使用 Android 5.1 镜像，证书校验宽松，直接把证书装为用户证书即可生效，省去系统证书步骤。</p></blockquote><blockquote><p><strong>MuMu 注意</strong>：连接 MuMu 时 ADB 端口是 <code>16384</code>，命令形如 <code>adb connect 127.0.0.1:16384</code>。</p></blockquote><h2 id="🚀-进阶技巧：搞定难缠的-App"><a href="#🚀-进阶技巧：搞定难缠的-App" class="headerlink" title="🚀 进阶技巧：搞定难缠的 App"></a>🚀 进阶技巧：搞定难缠的 App</h2><p>如果以上步骤完成，某些 App 仍然抓不到包，按下面策略逐个击破：</p><h3 id="1-Flutter-应用不走系统代理"><a href="#1-Flutter-应用不走系统代理" class="headerlink" title="1. Flutter 应用不走系统代理"></a>1. Flutter 应用不走系统代理</h3><p>Flutter 默认不遵循系统代理设置。需要在 Flutter 代码里手动配置代理，并加回调信任抓包证书：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Dio 示例</span></span><br><span class="line"><span class="keyword">final</span> dio = <span class="title class_">Dio</span>(</span><br><span class="line">  <span class="title class_">BaseOptions</span>(</span><br><span class="line">    baseUrl: <span class="string">&#x27;https://api.example.com&#x27;</span>,</span><br><span class="line">  ),</span><br><span class="line">);</span><br><span class="line">(dio.httpClientAdapter <span class="keyword">as</span> <span class="title class_">IOHttpClientAdapter</span>).createHttpClient = () &#123;</span><br><span class="line">  <span class="keyword">final</span> client = <span class="title class_">HttpClient</span>()</span><br><span class="line">    ..findProxy = (uri) =&gt; <span class="string">&#x27;PROXY 10.0.2.2:8888&#x27;</span>;</span><br><span class="line">  client.badCertificateCallback = (cert, host, port) =&gt; <span class="keyword">true</span>; <span class="comment">// 信任所有证书</span></span><br><span class="line">  <span class="keyword">return</span> client;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h3 id="2-绕过-App-的代理检测"><a href="#2-绕过-App-的代理检测" class="headerlink" title="2. 绕过 App 的代理检测"></a>2. 绕过 App 的代理检测</h3><p>部分 App 会检测系统代理，发现开了代理就拒绝联网。方案：<strong>关掉模拟器内的代理</strong>，改用 PC 端流量转发工具（如 <strong>Proxifier</strong>），把模拟器进程（如 <code>Nox.exe</code>、<code>MuMuPlayer.exe</code>）的流量<strong>强制转发</strong>到 Burp&#x2F;Charles 的监听端口。</p><h3 id="3-SSL-Pinning（证书固定）怎么办"><a href="#3-SSL-Pinning（证书固定）怎么办" class="headerlink" title="3. SSL Pinning（证书固定）怎么办"></a>3. SSL Pinning（证书固定）怎么办</h3><p>App 在代码里写死了服务器证书指纹，任何假证书都会被拒。两条路：</p><ul><li><strong>Xposed 框架</strong>：安装 Xposed + <strong>JustTrustMe</strong> 模块，从应用层 Hook 掉证书校验逻辑，强制信任抓包证书</li><li><strong>Frida</strong>：用 <code>frida-server</code> + 脚本注入绕过 <code>SSL_pinning</code>，适合原生 App 且无需重启</li></ul><h3 id="4-端口设置"><a href="#4-端口设置" class="headerlink" title="4. 端口设置"></a>4. 端口设置</h3><p>在抓包工具的 SSL 代理设置中，确保监听 <code>443</code>（HTTPS 默认端口），或直接配置 <code>*</code> 通配所有端口，避免 App 走非标端口时漏抓。</p><h2 id="🔍-常见问题排查"><a href="#🔍-常见问题排查" class="headerlink" title="🔍 常见问题排查"></a>🔍 常见问题排查</h2><table><thead><tr><th>现象</th><th>可能原因</th><th>解决</th></tr></thead><tbody><tr><td>模拟器断网</td><td>代理地址&#x2F;端口填错</td><td>确认宿主机 IP 与工具端口，<code>10.0.2.2</code> 仅在模拟器内部有效</td></tr><tr><td>抓到 CONNECT 但无明文</td><td>证书未生效</td><td>检查证书是否在系统目录、权限 644、已重启</td></tr><tr><td>App 提示证书错误</td><td>证书未被信任</td><td>确认 App 信任系统证书，或走 Xposed&#x2F;Proxifier 方案</td></tr><tr><td>部分流量缺失</td><td>App 不走系统代理</td><td>Flutter 手动配代理 &#x2F; Proxifier 强制转发</td></tr><tr><td>高版本 Android 装不上</td><td>用户证书不被信任</td><td>使用 Android 5.1~9 镜像，或 ROOT 后装系统证书</td></tr></tbody></table><h2 id="⚠️-合规提醒"><a href="#⚠️-合规提醒" class="headerlink" title="⚠️ 合规提醒"></a>⚠️ 合规提醒</h2><p>抓包本质上是对流量进行中间人解密，<strong>请务必只在授权的范围内使用</strong>：自己的 App、已授权的渗透测试项目、CTF 或靶场环境。对未授权目标抓包可能触犯法律，且抓取的数据可能涉及他人隐私，请注意保护。</p><hr><p>模拟器提供了一个高度可控的”沙盒”，虽然配置过程略显繁琐，但配置一次即可长期复用。按部就班完成这几步，你就能拿下大多数 App 的接口数据了。</p>]]></content>
      
      
      
        <tags>
            
            <tag> 安全研究 </tag>
            
            <tag> Android </tag>
            
            <tag> 抓包 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
